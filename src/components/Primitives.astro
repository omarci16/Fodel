---
/**
 * Not rendered — this file only documents where the prototype's small
 * components now live, so the mapping stays obvious during review.
 *
 *   Eyebrow    → components/Eyebrow.astro
 *   GoldRule   → components/GoldRule.astro
 *   BtnDark    → components/Button.astro   variant="dark"
 *   BtnOutline → components/Button.astro   variant="outline"
 *   TextBtn    → components/Button.astro   variant="text"
 *   Reveal     → class="reveal" + the shared observer in layouts/Base.astro
 *   Placeholder→ removed; every slot now takes a real image
 */
---
