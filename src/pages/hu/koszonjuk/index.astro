---
export const prerender = true;
/**
 * Confirmation page for submissions made without JavaScript.
 *
 * noindex: it is a transactional endpoint, not content.
 */
import Base from '~/layouts/Base.astro';
import Eyebrow from '~/components/Eyebrow.astro';
import GoldRule from '~/components/GoldRule.astro';
import Button from '~/components/Button.astro';
import { UI, path } from '~/i18n/ui';
import { COMPANY, formatHours } from '~/config/company';

const locale = 'hu' as const;
const ui = UI[locale];
const hu = locale === 'hu';
---

<Base
  locale={locale}
  title={hu ? 'Köszönjük a megkeresését' : 'Bedankt voor uw bericht'}
  description={hu ? 'Megkaptuk az üzenetét.' : 'Wij hebben uw bericht ontvangen.'}
  noindex
>
  <section class="thanks">
    <Eyebrow center space="18px">{hu ? 'Elküldve' : 'Verzonden'}</Eyebrow>
    <h1>{ui.form.successTitle}</h1>
    <GoldRule center space="32px" />
    <p class="body">{ui.form.successBody}</p>

    <p class="hours">{formatHours(locale)}</p>
    <p class="phones">
      {COMPANY.phones.slice(0, 2).map((p) => <a href={p.href}>{p.display}</a>)}
    </p>

    <div class="actions">
      <Button variant="dark" href={path(locale, 'properties')}>{ui.cta.allProperties}</Button>
      <Button variant="outline" href={`/${locale}/`}>{ui.nav.home}</Button>
    </div>
  </section>
</Base>

<style>
  .thanks { max-width: 640px; margin-inline: auto; padding: 100px var(--pad-x) var(--section-y); text-align: center; }
  h1 { font-family: var(--serif); font-weight: var(--fw-light); font-size: clamp(34px, 4.4vw, 62px); color: var(--ink); margin-bottom: 28px; }
  .body { font-size: var(--fs-body-lg); line-height: var(--lh-lead); color: var(--taupe); margin-bottom: 32px; }
  .hours { font-size: var(--fs-small); color: var(--taupe); }
  .phones { display: flex; gap: 20px; justify-content: center; flex-wrap: wrap; margin: 8px 0 40px; }
  .phones a { font-family: var(--serif); font-size: var(--fs-lead); color: var(--ink); text-decoration: underline; text-underline-offset: 4px; }
  .actions { display: flex; gap: 16px; justify-content: center; flex-wrap: wrap; }
  @media (max-width: 768px) {
    .thanks { padding: 56px var(--pad-x-mob) var(--section-y-mob); }
    .actions { flex-direction: column; }
  }
</style>
